# Followers and Followees

> This project analyzes a small directed social network represented in a text file and computes statistics like unique users, mutual relationships, and most-followed accounts. It is designed as a compact demonstration of parsing via tools like vectors and mapping.


## Tech Stack

- **Language:** C++ 
- **Standard Library:** `<vector>`, `<map>`, `<stack>`, `<queue>`, etc.
